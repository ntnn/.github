name: add-to-project

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}

permissions: {}

env:
  GH_TOKEN: ${{ secrets.PROJECT_TOKEN }}
  PROJECT_OWNER: ntnn
  PROJECT_ID: PVT_kwHOAKBvfc4BRoCe

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash ./add-to-project.bash
